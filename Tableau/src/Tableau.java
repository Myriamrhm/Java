import java.util.*;

public class Tableau {
        public static void main(String[] args) {
              /* int[] tableau = new int[4];
               	tableau[0] =152 ;
                tableau[1] = 5;
                tableau[2] = 596;
                tableau[3] = 63; */
        	
        	int[] tableau = {100, 70, 44, 40, 50};

                Arrays.sort(tableau);

                for(int i = 0; i < tableau.length; i++)
                        System.out.println(tableau[i]);
                        
        
        	
        }
        
}

